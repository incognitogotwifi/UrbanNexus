# BrowserQuest Build Tools

This directory contains build scripts and tools for BrowserQuest.

## Scripts

- `build.sh` - Main build script that creates optimized client builds

## Usage

To build the client for production:

```bash
./bin/build.sh
